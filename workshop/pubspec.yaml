name: hive_workshop_starter
description: "Workshop starter — UI ready, Hive NOT added yet"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2

  # ── TODO STEP 1: เพิ่ม Hive ตรงนี้ ──────────────────────────
  # hive: ^2.2.3
  # hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  # ── TODO STEP 1: เพิ่ม dev dependencies ตรงนี้ ───────────────
  # hive_generator: ^2.0.1
  # build_runner: ^2.4.6

flutter:
  uses-material-design: true
